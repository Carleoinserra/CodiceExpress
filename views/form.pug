doctype html
html(lang="en")
    head
        title Form Input
    body
        h1 Inserisci il tuo nome
        form(action="/submit", method="POST")
            label(for="nome") Nome:
            input(type="text", name="nome" id="nome")
            button(type="submit") Invia
